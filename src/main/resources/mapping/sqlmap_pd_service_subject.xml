<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pd_service_subject">



	<select id="queryPdServiceSubject" parameterType="net.chetong.order.model.PdServiceSubjectVO"
		resultType="net.chetong.order.model.PdServiceSubjectVO">
		SELECT
		id as id,
		service_id as serviceId,
		is_show as isShow,
		subject_name
		as subjectName,
		subject_desc as subjectDesc,
		ext1 as ext1,
		ext2 as ext2,
		ext3 as ext3,
		response_time as responseTime
		FROM pd_service_subject
		<where>
			<if test="id!= null">
				and id=#{id}
			</if>
			<if test="serviceId!= null">
				and service_id=#{serviceId}
			</if>
		</where>
		ORDER BY id DESC
	</select>
	
	<select id="getInsuranceType" resultType="net.chetong.order.model.InsuranceType">
		SELECT
			id as serviceId,
			service_name AS serviceName
		FROM
			pd_service p
		WHERE
			is_onsell = '1'
	</select>

</mapper>