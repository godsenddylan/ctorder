<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"  
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"  
    xsi:schemaLocation="http://www.springframework.org/schema/beans          
    http://www.springframework.org/schema/beans/spring-beans.xsd          
    http://code.alibabatech.com/schema/dubbo          
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->  
    <!-- <dubbo:application name="ctorder-consumer"/>  -->
    <dubbo:registry address="zookeeper://${zookeeper.address}"  timeout="100000"/>
    <dubbo:protocol name="dubbo" port="-1" />
    
    <dubbo:reference id="evComment" interface="com.chetong.aic.evaluate.api.remoting.EvComment" timeout="30000" check="false" retries="0" group="${dubbo.group}"/><!-- 和本地bean一样实现服务 -->
    <dubbo:reference id="pushMessageService" interface="com.chetong.ctwechat.service.PushMessageService" timeout="30000" check="false" retries="0" group="${dubbo.group}" async="true"/>   
	<dubbo:reference id="accountService" interface="com.chetong.aic.account.service.AccountNewApiService" timeout="30000" check="false" retries="0" group="${dubbo.group}"/>
	<!-- 短信接口 -->
    <dubbo:reference id="sysSmsService" interface="com.chetong.aic.api.remoting.sms.SysSmsService" timeout="30000" check="false" retries="0" group="${dubbo.group}" async="true"/>
    <!-- 调度服务接口 -->
    <dubbo:reference id="dcsApiService" interface="com.chetong.aic.api.remoting.dcs.DcsApiService" check="false" group="${dubbo.group}">
    	 <dubbo:method name="saveCaseRecord" async="true" return="false"></dubbo:method>
    </dubbo:reference>
</beans>
